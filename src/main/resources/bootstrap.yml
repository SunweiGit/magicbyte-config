spring:
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: "https://github.com/SunweiGit/magicbyte-config" #配置文件所在仓库
          username: "zhi1278300799@icloud.com"
          password: "*******"
          default-label: master #配置文件分支
          search-paths: "/config"  #配置文件所在根目录